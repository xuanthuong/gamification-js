extends ../layout

block styles

block content
    .container-fluid
        .col-md-6.col-sm-offset-3
                .card
                    .card-content
                        .col-md-12.frame
                            ul(id="chatbox")
                        .col-md-8.col-sm-offset-4(style="margin-bottom: 10px; ")
                                div
                                    .msj-rta.macro
                                        .text.text-r(style='background:whitesmoke !important')
                                            input.mytext(placeholder='Type a message')